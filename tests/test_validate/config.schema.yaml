$schema: "https://json-schema.org/draft/2020-12/schema"

description: ismk configuration file

type: object

properties:
  samples:
    type: string
  adapter:
    type: string
    pattern: "^[ACGT]+$"

required:
  - samples
