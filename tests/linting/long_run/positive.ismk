rule a:
    input:
        "test.csv"
    output:
        "test.out.csv"
    log:
        "logs/a.log"
    run:
        # a few lines of code are still ok
        import pandas as pd

        t = pd.read_csv(ismk.input[0])
        t.sort(inplace=True)

        t.to_csv(ismk.output[0])